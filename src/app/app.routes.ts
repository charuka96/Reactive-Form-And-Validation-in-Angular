import { Routes } from '@angular/router';
import { FormValidComponent } from './component/form-valid/form-valid.component';

export const routes: Routes = [
  {path:'',component:FormValidComponent}
];
